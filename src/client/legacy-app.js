// Temporary compatibility entry while the workspace runtime finishes moving to features.
import { startWorkspaceApp } from "./features/workspace/runtime/workspace-app-runtime.js";

startWorkspaceApp(document);
